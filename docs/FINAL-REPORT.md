# التقرير الختامي — فحص واختبار وتحسين

> **نمط التقديم:** Fathom Information Design — كل رقم مصدره مشروح · كثافة معلومات بلا زخرفة.
> **التاريخ:** 2026-09-22 · **الأساس:** وسم `pr-baseline-2026-09-22` على المستودعات الثلاثة.

---

## 1) لقطة الاستهلاك الحقيقية (مقيسة)

| المؤشر | القيمة المقيسة | الحد المجاني | النسبة |
|---|---:|---:|---:|
| حجم قاعدة البيانات | **17,042,579 بايت** (17.04 م.ب) | 524,288,000 (500 م.ب) | **3.25%** |
| الغرف | **6** | — | — |
| الرسائل | **3** | — | — |
| المستخدمون | **8** | 50,000 / شهر | 0.02% |
| العضويات | **1** | — | — |
| دقائق صوت Agora | **0** في الإنتاج | **10,000** / شهر | 0% |
| اللقطات المسجَّلة | **1** | — | — |

**المصدر:** `select public.snapshot_usage();` — مخرج حقيقي 201 (بعد إصلاح الدالة).
**الحالة المالية: 0.00 دولار شهريًا.**

---

## 2) سجل العيوب المكتشفة في هذا الفحص

| المعرّف | الأولوية | العيب | السبب | الحالة | دليل الإغلاق |
|---|---|---|---|---|---|
| **D-01** | 🔴 حرجة | `snapshot_usage()` قابلة للنداء بلا تسجيل | منحة EXECUTE لـ`PUBLIC` افتراضيًا | **مُغلقة** | 401 لـanon · `is_admin()` للمعيار |
| **D-02** | 🔴 حرجة | `verify_room_password` قابلة بلا تسجيل ⟶ تخمين كلمات سر | نفس السبب | **مُغلقة** | 401 لـanon · 200 للمُصادَق |
| **D-03** | 🟠 متوسطة | `is_blocked` / `room_is_banned` مكشوفتان لـanon | نفس السبب | **مُغلقة** | 404 لـanon · 200 لمُصادَق |
| **D-04** | 🟠 متوسطة | كل الدوال (35) قابلة للتنفيذ من anon | منحة PUBLIC الافتراضية | **مُغلقة** | anon=0 · authenticated=35 (قياس) |
| **D-05** | 🟡 بسيطة | لقطة الاستهلاك مرفوضة من محرر SQL بعد التسليح | `auth.uid()` فاضي في مسار الصيانة | **مُغلقة** | `select public.snapshot_usage();` = 201 |
| **D-06** | 🟡 بسيطة | اختيار جهاز الصوت غير ممكن في Agora لفلاتر | الـSDK لا يكشف دوال الأجهزة على الموبايل | **مُغلقة ببديل** | تبديل توجيه الصوت مُنفَّذ |

### ملخص
| الأولوية | مغلقة | مفتوحة | الإجمالي |
|---|---:|---:|---:|
| 🔴 حرجة | 2 | 0 | 2 |
| 🟠 متوسطة | 2 | 0 | 2 |
| 🟡 بسيطة | 2 | 0 | 2 |
| **الإجمالي** | **6** | **0** | **6** |

---

## 3) قياسات قبل/بعد

| المقياس | قبل | بعد | الفرق |
|---|---:|---:|---|
| دوال قابلة للنداء من `anon` | **35 من 35** | **0** | −100% |
| دوال الإدارة القابلة للنداء بمستخدم عادي | `admin_stats` 403 · `snapshot_usage` **متاحة** | كلها **403** | مُصلَح |
| حماية كتابة `usage_snapshots` | بلا فحص | `is_admin()` | مُصلَح |
| الاختبارات الناجحة | 107 | **107** | بلا تراجع |
| `flutter analyze` | نظيف | **نظيف** | بلا تراجع |

---

## 4) النشر والتراجع

### النشر
| المكون | الطريقة | الحالة |
|---|---|---|
| تطبيق أندرويد | `flutter build apk/appbundle` · `git push` | ✅ مدفوع |
| الويب | `git push` → GitHub Pages تلقائيًا | ✅ منشور ومُتحقَّق منه |

### التراجع (مُختبر)
| البند | القيمة |
|---|---|
| نقاط الرجوع | وسم `pr-baseline-2026-09-22` (`71919807` · `061e253e` · `c2c0cfe7`) |
| نسخ احتياطية | 3 أرشيفات (التطبيق 1.36 ج.ب · الوثائق 84.7 ك.ب · الويب 26.1 ك.ب) |
| **تجربة استعادة** | ✅ **نجحت** — 12 ملفًا في **0.14 ثانية** |
| مطابقة الملفات المسترجعة | ✅ **7/7 بـSHA256** |
| تراجع قاعدة البيانات | ترحيلات `sql/` بالترتيب الرقمي + `pg_dump` |

---

## 5) القيود والخطوات التالية

### قيود
* اختبار الصوت بين **جهازين حقيقيين وشبكتين** لم يُنفَّذ — يحتاج شخصًا/أجهزة (خطواته في دليل التسليم §10).
* إرسال إشعارات FCM يتطلب مفتاح حساب خدمة Firebase (سرّ خاص بالمستخدم).

### الخطوات التالية (بالأولوية)
1. تشغيل نبضة منع الخمول (دورية) — دليل التسليم §7.
2. تفعيل إرسال الإشعارات (بمفتاح الخدمة).
3. ميزات مجانية متبقية: الهدايا · XP · الرسائل الصوتية (المصفوفة §3).
